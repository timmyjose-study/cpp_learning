vector_iterator = executable('vector_iterator', 'vector_iterator.cpp')
next_iterator = executable('next_iterator', 'next_iterator.cpp')
tokenizer = executable('tokenizer', 'tokenizer.cpp')

test('vector_iterator', vector_iterator)
test('next_iterator', next_iterator)
test('tokenizer', tokenizer)
